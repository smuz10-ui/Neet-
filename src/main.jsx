import React from "react";
import ReactDOM from "react-dom/client";
import PyqWebsite from "./PyqWebsite";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PyqWebsite />
  </React.StrictMode>
);
